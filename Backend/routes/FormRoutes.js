// formRoutes.js (Backend)
const express = require('express');
const router = express.Router();

// Example route handler for submitting a form
router.post('/form', (req, res) => {
  // Logic for submitting a form
});

// Example route handler for retrieving forms
router.get('/forms', (req, res) => {
  // Logic for retrieving forms
});

module.exports = router;
